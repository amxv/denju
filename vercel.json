{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "installCommand": "bun install --frozen-lockfile --filter @denju/docs",
  "buildCommand": "bun run docs:build",
  "outputDirectory": "docs/dist",
  "ignoreCommand": "node docs/scripts/should-build.mjs"
}
